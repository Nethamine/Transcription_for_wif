# ─────────────────────────────────────────────────────────────────────────────
# Whisper transcription app — Python dependencies (target: Python 3.12)
# ─────────────────────────────────────────────────────────────────────────────
# Torch is left UNPINNED on purpose. The CPU index below supplies "+cpu" wheels,
# and pip prefers them automatically, so you get the lean CPU build that matches
# whatever Python version is active — no version-not-found errors.
#
#   ▸ Have a CUDA GPU? Delete the --extra-index-url line so pip installs the
#     GPU-enabled build from PyPI instead.
# ─────────────────────────────────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu

torch
torchaudio

streamlit
openai-whisper

# Stay on pyannote 3.x — 4.0 is an API rewrite that would break
# merge_whisper_diarization() and the token= auth call as written.
pyannote.audio>=3.1,<4

soundfile